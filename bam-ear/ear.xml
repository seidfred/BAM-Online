<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>de.seidfred.bam</groupId>
    <artifactId>bam</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <!-- EarProj ohne <groupId> und <version>: Wird von <parent> uebernommen: -->
  <artifactId>bam-ear</artifactId>
  <packaging>ear</packaging>
  <name>bam-ear</name>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-ear-plugin</artifactId>
        <version>2.5</version>
        <configuration>
          <defaultLibBundleDir>lib</defaultLibBundleDir>
          <archive>
            <manifest>
              <addClasspath>true</addClasspath>
            </manifest>
          </archive>
          <modules>
            <ejbModule>
              <groupId>${project.groupId}</groupId>
              <artifactId>bam-ejb</artifactId>
              <bundleFileName>bam-ejb.jar</bundleFileName>
            </ejbModule>
            <webModule>
              <groupId>${project.groupId}</groupId>
              <artifactId>bam-web</artifactId>
              <bundleFileName>bam-web.war</bundleFileName>
              <contextRoot>/bam-web</contextRoot>
            </webModule>
          </modules>
        </configuration>
      </plugin>
      <!--
      <plugin>
        <groupId>org.glassfish.maven.plugin</groupId>
        <artifactId>maven-glassfish-plugin</artifactId>
        <version>2.1</version>
        <configuration>
          <domain>
            <name>domain1</name>
            <adminPort>4848</adminPort>
          </domain>
          <glassfishDirectory>${env.GLASSFISH_HOME}</glassfishDirectory>
          <user>admin</user>
          <adminPassword>adminadmin</adminPassword>
          <echo>true</echo>
        </configuration>
      </plugin>
      -->
      <!--
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jboss-maven-plugin</artifactId>
        <configuration>
          <jbossHome>${env.JBOSS_HOME}</jbossHome>
          <port>8080</port>
        </configuration>
      </plugin>
      -->
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>bam-ejb</artifactId>
      <version>${project.version}</version>
      <type>ejb</type>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>bam-web</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>
  </dependencies>
</project>